# QuestNavLib Build Configuration

# Version Configuration (can be overridden via -P flags)
questnavVersion=2025-1.0.0
releaseType=dev
frcYear=2025
wpilibVersion=2025.3.2

# Build Configuration
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Publishing Configuration (set via command line or CI)
mavenRepoUrl=https://maven.questnav.gg/repository
# mavenUsername=
# mavenPassword=

# Java Configuration
java.incremental=true